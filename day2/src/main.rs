fn load_input(input_path: &str) -> () {
    let file = std::fs::File::open(input_path).expect("Failed to open input_path");
    let reader = std::io::BufReader::new(file);
}

fn main() {
    p = "/Users/zacswider/code/AOC2024/day2/days2.txt";
}
